mixin header(options={})
  - let { login = true, user = "Юлий Цезарь", list } = options;

  header.header
    .header-main
      .header-main__container
        .header-main__logo
          +logo
          +burger

        nav.header-main__nav
          +header-nav(list)

          .header-main__items

          if login
            .header-main__button
              .header-main__button.header-main__left-button
                +button({text: "Войти",mods: 'border,content-size'})
              .header-main__button.header-main__right-button
                +button({text: 'Зарегистрироваться'})

          else
            .header-main__person= user
