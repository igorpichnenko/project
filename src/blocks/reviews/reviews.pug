mixin reviews({number,name,check}={})
  .reviews
    +rate-button({newName: name,checked: check})
    .reviews-container
      .reviews-container__number= number
      .reviews-container__text Отзывов
