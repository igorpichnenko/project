mixin link(classname,...items)  
            ul(class=""+classname)
               each item in items
                 li(class="footer-nav__text")= item
                  a(href="#").footer-nav__link